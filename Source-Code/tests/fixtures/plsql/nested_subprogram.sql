PROCEDURE OUTER_PROC (P_ID IN NUMBER) IS
  V_COUNT NUMBER := 0;
  PROCEDURE INNER_HELPER (P_X IN NUMBER) IS
  BEGIN
    V_COUNT := V_COUNT + P_X;
  END INNER_HELPER;
  V_TOTAL NUMBER;
BEGIN
  INNER_HELPER(P_ID);
  V_TOTAL := V_COUNT;
END OUTER_PROC;
