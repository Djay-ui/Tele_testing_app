PROCEDURE UTIL_CALLS (P_CLOB IN VARCHAR2) IS
  V_LEN NUMBER;
  V_PART VARCHAR2(100);
  V_RAND NUMBER;
BEGIN
  V_LEN := DBMS_LOB.GETLENGTH(P_CLOB);
  V_PART := DBMS_LOB.SUBSTR(P_CLOB, 10, 5);
  V_RAND := DBMS_RANDOM.VALUE;
  DBMS_OUTPUT.PUT_LINE('Length: ' || V_LEN);
END UTIL_CALLS;
